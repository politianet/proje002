<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="30dp"
    tools:context=".AracUpdateDialogFragment">

    <EditText
        android:id="@+id/pilakaEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="PİLAKA"
        android:inputType="text"/>

    <EditText
        android:id="@+id/markaEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pilakaEditText"
        android:hint="MARKA"
        android:inputType="textPersonName" />


    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:weightSum="2"
        android:layout_below="@+id/markaEditText"
        android:orientation="horizontal">

        <Button
            android:id="@+id/guncelleAracInfoButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Güncelle"
            android:background="@drawable/button_add"
            android:textColor="@color/colorWhite"
            />

        <Button
            android:background="@drawable/button_cancel"
            android:textColor="@color/colorWhite"
            android:id="@+id/vazgecAracInfoButton"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Vazgeç"
            />
    </LinearLayout>


</RelativeLayout>